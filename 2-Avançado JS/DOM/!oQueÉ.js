// A forma em que o navegador lê o documento HTML e armazena na memória
// Nós programadores podemos manipular objetos para armazenar nessa memória

// Alterações DOM é armazenado na memória, então, caso recarregue a página
// toda a alteração será removida e o site voltará ao padrão, porque ele não
// altera os dados no servidor e nem no código, apenas armazena na memória do
// navegador.

// DOM (Document Object Model) é a representação de dados dos objetos que compõem
// a estrutura e o conteúdo de um documento na Web.

// Representa a página de "forma que os programas possam alterar a estrutura do
// documento, alterar o estilo e conteúdo". O DOM representa o documento com nós
// e objetos, dessa forma, as linguagens de programação podem se conectar à página.

// DOM na prática?
// Alterar e criar elementos HTML
// Adicionar eventos e validações
// Permite a existência de frameworks, como React.js, Vue e Angular
