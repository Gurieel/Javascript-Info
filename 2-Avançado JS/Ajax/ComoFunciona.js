// Ajax, uma forma inteligente de fazer chamadas de dados assíncronas, chamadas para o servidor.

// Asynchronous JavaScript And XML (AJAX) é uma tecnologia utilizada para realizar comunicações
// entre navegador e servidor "sem sair da página".

// Em casos que não possuem AJAX, é como exemplo o instagram, para atualizar o feed é necessário
// dar F5 na página para que atualize as informações e os novos dados.
// Com AJAX tudo que for atualizado e mandado para o servidor, alterará sem precisar atualizar
// a página.

// JavaScript(navegador) > AJAX(Utilizando JSON) > Node.js(servidor)
